##############################
## POSTGRESQL BACKUP CONFIG ##
##############################

# System user that should run the backup scripts.
# If non-empty and the current user does not match, the scripts will abort.
BACKUP_USER=postgres

# PostgreSQL connection settings.
# If empty, sensible defaults will be used by the scripts.
HOSTNAME=localhost
USERNAME=postgres
#PORT=5432     # Uncomment and adjust if you need a non-default port

# Directory where all backups will be stored.
# This directory will be created automatically if it does not exist.
BACKUP_DIR=/data/pgbackup

# Comma-separated list of databases that should be dumped as *schema-only*.
# Example: SCHEMA_ONLY_LIST="template1,template0"
SCHEMA_ONLY_LIST=

# Toggle backup formats.
# Plain backups are SQL dumps, optionally gzipped by the scripts.
ENABLE_PLAIN_BACKUPS=yes

# Custom backups use pg_dump -Fc (custom format).
# This is useful for selective restores with pg_restore.
ENABLE_CUSTOM_BACKUPS=no

# Backup PostgreSQL globals (roles, permissions, tablespaces) using pg_dumpall -g.
# For a simple single-host NetBox deployment this is often optional.
ENABLE_GLOBALS_BACKUPS=no


#### SETTINGS FOR ROTATED BACKUPS ####

# On which weekday a *weekly* backup should be created.
# 1-7 = Monday-Sunday (ISO-8601, as returned by `date +%u`)
DAY_OF_WEEK_TO_KEEP=7

# Number of days to keep *daily* backups.
DAYS_TO_KEEP=14

# Number of weeks to keep *weekly* backups.
WEEKS_TO_KEEP=8

######################################
